# Sources

Adapted on 12 August 2026 from the `patterson-agencies-marketplace` catalog entry
`pdco-animal-health-intl` (skill `bootstrap-animal-health-intl`), which is generated from
that repository's `catalog/` definitions.

| Content | Origin | Confidence |
| --- | --- | --- |
| Company facts (acquisition history, customer mix) | Agencies catalog `business-units.ts` context entries, publicly reported Patterson history | High — matches public record; confirm during interview |
| Standing interview sections | Agencies catalog `pdco-core` skill `interview-for-company-setup`, restructured and inlined here because this repository has no `pdco-core` dependency | High — method content, no factual claims |
| Known gaps list | The `[TBD]` markers carried by the catalog entry, plus the Fabric workspace inventory gap added in this fork | High — gaps are deliberate |
| Output procedure | Rewritten for this hand-maintained repository (the agencies repo emits `plugins/` from a generator; this one does not) | High — describes this repo's own workflow |
